services:
  - type: web
    name: iot-dashboard
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: iot-db
          property: connectionString

databases:
  - name: iot-db
    databaseName: iot
    user: iot
    plan: free
